package:
  name: geometric
  version: "1.0.1"

source:
  path: ../..

build:
  number: 0

requirements:
  host:
    - python
    - setuptools

  run:
    - python
    - numpy {{ numpy }}
    - networkx
    - six

test:
  requires:
    - pytest
  imports:
    - geometric

about:
  home: https://github.com/leeping/geomeTRIC
  license: 
