name: test
channels:
  - conda-forge
  - psi4
dependencies:
    # Psi test depends
  - psi4
  - qcengine

    # geomeTRIC base depends
  - numpy
  - networkx

    # Testing
  - pytest
  - codecov
  - pytest-cov
