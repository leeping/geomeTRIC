name: test
channels:
  - psi4
  - rdkit
  - conda-forge
  - defaults
dependencies:
  # RDKit test depends
  - rdkit

  # Psi test depends
  - psi4 >= 1.6.1
  - qcengine >= 0.24

  # OpenMM test depends
  - openmm

  # geomeTRIC base depends
  - python=3.9
  - numpy
  - networkx

  # Testing
  - pytest
  - codecov
  - pytest-cov
