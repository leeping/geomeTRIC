name: test
channels:
  - psi4
  - rdkit
  - defaults
  - conda-forge
dependencies:
  # ASE tests
  - ase

  # RDKit test depends
  - rdkit

  # Psi test depends
  - psi4 >= 1.6.1
  - qcengine >= 0.24

  # OpenMM test depends
  - openmm

  # geomeTRIC base depends
  - numpy
  - networkx

  # Testing
  - pytest
  - codecov
  - pytest-cov
