name: test
channels:
  - conda-forge
  - psi4
  - molssi
dependencies:
    # Psi test depends
  - psi4

    # geomeTRIC base depends
  - numpy
  - networkx

    # Testing
  - pytest
  - codecov
  - pytest-cov
  - pip:
    - git+git://github.com/MolSSI/QCEngine#egg=qcengine
