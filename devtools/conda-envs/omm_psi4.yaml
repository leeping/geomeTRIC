name: test
channels:
  - defaults
  - psi4
  - omnia
  - conda-forge
dependencies:
    # Psi test depends
  - psi4 >= 1.3
  - qcengine >= 0.15

    # OpenMM test depends
  - openmm

    # geomeTRIC base depends
  - numpy
  - networkx

    # Testing
  - pytest
  - codecov
  - pytest-cov
