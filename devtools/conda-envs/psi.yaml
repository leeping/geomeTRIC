name: test
channels:
  - conda-forge
  - rdkit
  - molssi
dependencies:
    # Psi test depends
  - rdkit
  - qcengine

    # geomeTRIC base depends
  - numpy
  - networkx

    # Testing
  - pytest
  - codecov
  - pytest-cov
