name: test
channels:
  - defaults
  - psi4
  - conda-forge
dependencies:
    # Psi test depends
  - psi4
  - qcengine

    # geomeTRIC base depends
  - numpy
  - networkx

    # Testing
  - pytest
  - codecov
  - pytest-cov
