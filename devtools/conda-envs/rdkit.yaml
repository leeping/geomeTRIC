name: test
channels:
  - conda-forge
  - omnia
  - rdkit
dependencies:
    # RDKit test depends
  - rdkit
  - qcengine >=0.6*

    # OpenMM test depends
  - openmm

    # geomeTRIC base depends
  - numpy
  - networkx

    # Testing
  - pytest
  - codecov
  - pytest-cov
