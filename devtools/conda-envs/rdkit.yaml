name: test
channels:
  - conda-forge
  - rdkit
dependencies:
    # RDKit test depends
  - rdkit
  - qcengine

    # geomeTRIC base depends
  - numpy
  - networkx

    # Testing
  - pytest
  - codecov
  - pytest-cov
