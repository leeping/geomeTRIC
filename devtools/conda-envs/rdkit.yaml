name: test
channels:
  - conda-forge
  - rdkit
  - molssi
dependencies:
    # RDKit test depends
  - rdkit

    # geomeTRIC base depends
  - numpy
  - networkx

    # Testing
  - pytest
  - codecov
  - pytest-cov
  - pip:
    - git+git://github.com/MolSSI/QCEngine@v0.4.0#egg=qcengine
