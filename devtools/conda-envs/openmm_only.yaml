name: test
channels:
  - conda-forge
  - omnia
dependencies:
    # OpenMM test depends
  - openmm

    # geomeTRIC base depends
  - numpy
  - networkx

    # Testing
  - pytest
  - codecov
  - pytest-cov

    # Python 2.7 specific
  - future
